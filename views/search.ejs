<!DOCTYPE html>
<html>
  <head>
    <title>Food</title>
    <% include templates/header %>
    <link rel="stylesheet" type="text/css" href="/stylesheets/search.css"/>
  </head>
  <body>
    <% include templates/navbar %>
    <h1>Food search</h1>
    <ul>
        <% for (var i = 0; i < results.length; i++) { %>
            <% if (results[i].brand_name) { %>
            <li><%= results[i].food_name %> - <%= results[i].brand_name %></li>
            <% } else { %>
                <li><%= results[i].food_name %></li>                
            <% } %>
            <ul>
                <% for (var j = 0; j < descriptions[i].length; j++) { %>
                    <li><%= descriptions[i][j] %></li>
                <% } %>
            </ul>
            <form action="/food/add" method="post">
                <label for="food_name">Food name:</label>
                <input class="food-input" type="text" id="food_name" name="food_name" value="<%= results[i].food_name %>" />
                
                <label for="brand_name">Brand Name:</label>
                <input class="food-input" type="text" id="brand_name" name="brand_name" value="<%= results[i].brand_name %>" />
         
                <label for="calories">Calories:</label>
                <input class="food-input" type="text" id="calories" name="calories" value="<%= descriptions[i][0]%>" />
         
                <label for="protein">Protein:</label>
                <input class="food-input" type="text" id="protein" name="protein" value="<%= descriptions[i][1]%>" />

                <label for="carbs">Carbs:</label>
                <input class="food-input" type="text" id="carbs" name="carbs" value="<%= descriptions[i][2]%>" />
     
                <label for="fat">Fat:</label>
                <input class="food-input" type="text" id="fat" name="fat" value="<%= descriptions[i][3]%>" />
        
                <input class="btn btn-success btn-sm" type="submit" value="Add to Log" />
            </form>
        <% } %>
    </ul>
  </body>
</html>